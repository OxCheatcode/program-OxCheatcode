
# Solana Escrow Frontend
This frontend is a Next.js + TypeScript starter configured with Solana Wallet Adapter.
- Run: `npm install` then `npm run dev`
- Ensure you set NEXT_PUBLIC_PROGRAM_ID and NEXT_PUBLIC_CLUSTER in .env.local
